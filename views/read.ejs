<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheet/style.css" >
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 text-white p-10" >
        <div class="nav w-1/6 rounded-lg p-2 text-center m-2 bg-blue-500" >
            <a href="/" >Back to home</a>
           </div>
        <h1 class="text-3xl mb-3 tracking-tighter" >All User </h1>
    <div class="users flex gap-3 flex-wrap" >
        <%if(users.length > 0){%>
    <% users.forEach(function(user){ %>          
<div class="user w-60 p-4 bg-zinc-800 rounded-lg" >
    <div class="w-full h- rounded-lg bg-zinc-600 overflow-hidden" >
                <img src="<%=user.image%>" class="w-full h-full object-position-top object-fit-cover " alt="" />
            </div>
            <div class="text-xl mt-3 tracking-tighter" ><%=user.name%></div>
            <div class="text-zinc-500" ><%=user.email%></div>    
            <div class="flex gap-3 mt-10" >
                <a class="text-yellow-500" href="" >Edit User</a>
                <a class="text-red-500" href="/delete/<%=user._id%>" >Delete User</a>
            </div>
        </div>
    <% })%>
    <%} else { %>
    
        <div class="text-center  text-2xl text-zinc-500" >User not created yet!</div>

    <% } %>
   
    
</div>

</div>
</body>
</html>